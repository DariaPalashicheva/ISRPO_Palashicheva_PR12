<Page x:Class="ISRPO_Palashicheva_PR12.PageMain.PageLogin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ISRPO_Palashicheva_PR12.PageMain"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="450"
      Title="Авторизация">

    <Grid>
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Введите логин" Style="{StaticResource Label}"/>
            <TextBox Style="{StaticResource TextBox}" x:Name="txbLogin"/>
            <Label Content="Введите пароль" Style="{StaticResource Label}"/>
            <PasswordBox Style="{StaticResource PasswordBox}" x:Name="psbPassword"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Регистрация" Style="{StaticResource ButtonY}" Click="btnRegIn_Click"/>
                <Button Content="Войти" Style="{StaticResource ButtonG}" Click="btnIn_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
